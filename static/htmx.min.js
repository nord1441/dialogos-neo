// Placeholder. In production drop in https://unpkg.com/htmx.org/dist/htmx.min.js
// (the htmx project, MIT licensed). This file is loaded by base.html so the
// app is functional even without it; the message composer uses a small
// hand-rolled fetch+SSE handler in chatapp-sse.js that works without htmx.
